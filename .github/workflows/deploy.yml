name: Root

on:
  push:
    branches: [ "test" ]

env:
  REGISTRY: ghcr.io
  IMAGE: ${{ github.repository }}

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Log in to the Container registry
        run: echo ${{ secrets.GITHUB_TOKEN }} | docker login $REGISTRY -u $ --password-stdin

      - name: Build Docker image
        run: docker build -t $REGISTRY/$IMAGE .

      - name: Push docker image
        run: docker push $REGISTRY/$IMAGE

      - name: Update import map
        uses: fjogeleit/http-request-action@v1
        with:
          url: 'https://im.nimbus.mdev.kz/services/?env=dev'
          method: 'PATCH'
          username: "imdadmin"
          password: "2&MrV1u1A3<="
          data: >
            {
              "service": "@main",
              "url": "https://dev.main.nimbus.mdev.kz/main.js?t=${{ env.GITHUB_SHA }}"
            }